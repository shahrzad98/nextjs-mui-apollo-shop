const { makeVar } = require('@apollo/client');
export const openModal = makeVar(false);
export const setImage = makeVar({});
export const snackbarsState = makeVar({});
export const customizationParent = makeVar(undefined);
